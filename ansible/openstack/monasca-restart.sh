ansible compute3 -m shell -i openstack-inventory -a "systemctl restart monasca-agent.target"
ansible compute4 -m shell -i openstack-inventory -a "systemctl restart monasca-agent.target"
ansible compute5 -m shell -i openstack-inventory -a "systemctl restart monasca-agent.target"
ansible compute6 -m shell -i openstack-inventory -a "systemctl restart monasca-agent.target"
ansible compute7 -m shell -i openstack-inventory -a "systemctl restart monasca-agent.target"
ansible compute8 -m shell -i openstack-inventory -a "systemctl restart monasca-agent.target"
ansible compute9 -m shell -i openstack-inventory -a "systemctl restart monasca-agent.target"
ansible compute10 -m shell -i openstack-inventory -a "systemctl restart monasca-agent.target"
ansible compute11 -m shell -i openstack-inventory -a "systemctl restart monasca-agent.target"
ansible compute12 -m shell -i openstack-inventory -a "systemctl restart monasca-agent.target"
ansible compute13 -m shell -i openstack-inventory -a "systemctl restart monasca-agent.target"
ansible compute14 -m shell -i openstack-inventory -a "systemctl restart monasca-agent.target"
ansible compute15 -m shell -i openstack-inventory -a "systemctl restart monasca-agent.target"
ansible compute16 -m shell -i openstack-inventory -a "systemctl restart monasca-agent.target"
ansible compute17 -m shell -i openstack-inventory -a "systemctl restart monasca-agent.target"
ansible compute18 -m shell -i openstack-inventory -a "systemctl restart monasca-agent.target"
ansible compute19 -m shell -i openstack-inventory -a "systemctl restart monasca-agent.target"
ansible compute20 -m shell -i openstack-inventory -a "systemctl restart monasca-agent.target"
ansible compute21 -m shell -i openstack-inventory -a "systemctl restart monasca-agent.target"
ansible compute22 -m shell -i openstack-inventory -a "systemctl restart monasca-agent.target"
ansible compute23 -m shell -i openstack-inventory -a "systemctl restart monasca-agent.target"
ansible compute24 -m shell -i openstack-inventory -a "systemctl restart monasca-agent.target"
